import 'package:flutter/material.dart';

class AdminApprovalTab extends StatelessWidget {
  const AdminApprovalTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('승인 대기 화면은 관리자 관리에서 열 수 있습니다.'),
    );
  }
}
