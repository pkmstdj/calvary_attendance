import 'package:flutter/material.dart';

class ChoirTeamTab extends StatelessWidget {
  const ChoirTeamTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('성가대 화면'),
      ),
    );
  }
}
