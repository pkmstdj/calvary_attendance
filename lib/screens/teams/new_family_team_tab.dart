import 'package:flutter/material.dart';

class NewFamilyTeamTab extends StatelessWidget {
  const NewFamilyTeamTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('새가족팀 화면'),
      ),
    );
  }
}
