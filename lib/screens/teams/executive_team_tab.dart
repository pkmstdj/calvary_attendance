import 'package:flutter/material.dart';

class ExecutiveTeamTab extends StatelessWidget {
  const ExecutiveTeamTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('임원 화면'),
      ),
    );
  }
}
