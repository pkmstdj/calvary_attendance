import 'package:flutter/material.dart';

class PraiseTeamTab extends StatelessWidget {
  const PraiseTeamTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('찬양팀 화면'),
      ),
    );
  }
}
