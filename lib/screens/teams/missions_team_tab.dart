import 'package:flutter/material.dart';

class MissionsTeamTab extends StatelessWidget {
  const MissionsTeamTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('선교팀 화면'),
      ),
    );
  }
}
