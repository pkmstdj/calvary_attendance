import 'package:flutter/material.dart';

class WorshipTeamTab extends StatelessWidget {
  const WorshipTeamTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('워십팀 화면'),
      ),
    );
  }
}
