import 'package:flutter/material.dart';

class GuidanceSupportTeamTab extends StatelessWidget {
  const GuidanceSupportTeamTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('안내&지원팀 화면'),
      ),
    );
  }
}
